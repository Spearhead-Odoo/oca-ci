name: ci

on:
  workflow_dispatch:
  push:
    branches:
      - master-sh
  pull_request:
  schedule:
    - cron: "0 4 * * 1"

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 16
      fail-fast: false
      matrix:
        include:
          # odoo/odoo
          - python_version: "3.12"
            codename: jammy
            chrome_milestone: 139
            odoo_version: "19.0"
            odoo_org_repo: "odoo/odoo"
            odoo_enterprise_org_repo: "Spearhead-Odoo/enterprise"
            image_name: py3.12-odoo19.0
          - python_version: "3.12"
            codename: jammy
            chrome_milestone: 139
            odoo_version: "18.0"
            odoo_org_repo: "odoo/odoo"
            odoo_enterprise_org_repo: "Spearhead-Odoo/enterprise"
            image_name: py3.12-odoo18.0
          - python_version: "3.10"
            codename: jammy
            chrome_milestone: 139
            odoo_version: "17.0"
            odoo_org_repo: "odoo/odoo"
            odoo_enterprise_org_repo: "Spearhead-Odoo/enterprise"
            image_name: py3.10-odoo17.0
          - python_version: "3.7"
            codename: jammy
            chrome_milestone: 126
            odoo_version: "13.0"
            odoo_org_repo: "odoo/odoo"
            odoo_enterprise_org_repo: "Spearhead-Odoo/enterprise"
            image_name: py3.7-odoo13.0
    services:
      postgres:
        image: postgres:14.0
        env:
          POSTGRES_USER: odoo
          POSTGRES_PASSWORD: odoo
          POSTGRES_DB: odoo
        ports:
          - 5432:5432
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build image
        uses: docker/build-push-action@v6
        with:
          file: "Dockerfile${{ matrix.python_version == '3.7' && '-legacypy' || '' }}"
          build-args: |
            codename=${{ matrix.codename }}
            chrome_milestone=${{ matrix.chrome_milestone }}
            python_version=${{ matrix.python_version }}
            odoo_version=${{ matrix.odoo_version }}
            odoo_org_repo=${{ matrix.odoo_org_repo }}
            odoo_enterprise_org_repo=${{ matrix.odoo_enterprise_org_repo }}
            GIT_TOKEN=${{ secrets.GIT_TOKEN }}
          tags: |
            ghcr.io/spearhead-odoo/oca-ci/${{ matrix.image_name }}:latest
          labels: |
            org.opencontainers.image.source=${{ github.event.repository.html_url }}
          cache-from: type=registry,ref=ghcr.io/spearhead-odoo/oca-ci/${{ matrix.image_name }}:latest
          cache-to: type=local,dest=/tmp/.buildx-cache
          load: true
      - name: Checkout
        uses: actions/checkout@v4
      - name: Tests
        run: docker run -v ${PWD}/tests:/mnt/tests --network=host -e PGHOST=localhost ghcr.io/spearhead-odoo/oca-ci/${{ matrix.image_name }}:latest /mnt/tests/runtests.sh -v
        # Don't bother with Odoo 8 and 9 for now...
        if: ${{ matrix.odoo_version != '8.0' && matrix.odoo_version != '9.0' }}
      - name: Chrome version
        run: docker run ghcr.io/spearhead-odoo/oca-ci/${{ matrix.image_name }}:latest /opt/chrome-linux64/chrome --version
      - name: Push image
        uses: docker/build-push-action@v6
        with:
          file: "Dockerfile${{ matrix.python_version == '3.7' && '-legacypy' || '' }}"
          build-args: |
            codename=${{ matrix.codename }}
            chrome_milestone=${{ matrix.chrome_milestone }}
            python_version=${{ matrix.python_version }}
            odoo_version=${{ matrix.odoo_version }}
            odoo_org_repo=${{ matrix.odoo_org_repo }}
            odoo_enterprise_org_repo=${{ matrix.odoo_enterprise_org_repo }}
            GIT_TOKEN=${{ secrets.GIT_TOKEN }}
          tags: |
            ghcr.io/spearhead-odoo/oca-ci/${{ matrix.image_name }}:latest
          labels: |
            org.opencontainers.image.source=${{ github.event.repository.html_url }}
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=inline
          push: true
        if: ${{ github.repository_owner == 'Spearhead-Odoo' && github.ref == 'refs/heads/master-sh' }}
